# UX 倡導者視角報告

> 視角：UX 倡導者
> 聚焦：使用者體驗、CLI 設計、開發者體驗

## CLI 設計

### 觸發命令

```bash
# 主要命令
/multi-tasks [plan-id]

# 範例
/multi-tasks user-auth
/multi-tasks --from-plan user-auth
/multi-tasks --quick user-auth
/multi-tasks --deep user-auth
```

### Flags 設計

```bash
# 模式控制
--quick             # 快速模式（2 視角）
--deep              # 深度模式（6 視角）
--perspectives N    # 自訂視角數

# 輸入控制
--from-plan ID      # 指定 plan ID
--no-memory         # 不存檔

# TDD 控制
--tdd               # 強制測試優先
--no-tdd            # 不強制測試優先

# 輸出控制
--format yaml|json  # 輸出格式
--dry-run           # 預覽不執行
```

### 互動流程

```
$ /multi-tasks user-auth

🔍 載入計劃：user-auth
   來源：.claude/memory/plans/user-auth/implementation-plan.md

📊 計劃摘要：
   - 3 個里程碑
   - 預估工作量：8h

❓ 選擇執行模式：
   1. 快速模式（2 視角，適合小功能）
   2. 標準模式（4 視角，推薦）
   3. 深度模式（6 視角，適合大功能）

   選擇 [2]:

🚀 開始任務分解...

   ┌─ Phase 1: 依賴分析 ─────────────────────────┐
   │ 🔄 dependency-analyst 分析中...             │
   │ ✅ 完成：識別 12 個依賴關係                  │
   └─────────────────────────────────────────────┘

   ┌─ Phase 2: 並行分解 ─────────────────────────┐
   │ 🔄 task-decomposer   [=====>    ] 60%       │
   │ 🔄 test-planner      [======>   ] 70%       │
   │ 🔄 risk-preventor    [====>     ] 50%       │
   └─────────────────────────────────────────────┘

✅ 任務分解完成！

📋 分解結果：
   - 總任務數：15
   - 功能任務：6
   - 測試任務：5
   - 風險任務：4

🔀 執行計劃：
   Wave 1: TEST-001, T-F-001 (可並行)
   Wave 2: T-F-002, TEST-002
   Wave 3: T-F-003, T-F-004, RISK-001

📁 已存檔：
   .claude/memory/tasks/user-auth/tasks.yaml
```

## 輸出格式

### 成功輸出

```
✅ 任務分解完成：{feature-id}

📊 分解摘要：
   - {N} 個視角完成
   - {N} 個任務產出
   - {N} 個 Wave 分組
   - 預估總時長：{duration}

📋 任務分佈：
   - 功能任務 (T-F-*): {N} 個
   - 測試任務 (TEST-*): {N} 個
   - 風險任務 (RISK-*): {N} 個

🎯 優先級分佈：
   - P0 必做: {N} 個
   - P1 應做: {N} 個
   - P2 可延: {N} 個

🔗 關鍵路徑：
   {task-id} → {task-id} → {task-id}
   預估：{duration}

📁 已存檔：
   .claude/memory/tasks/{feature-id}/tasks.yaml

🔗 快速連結：
   - 任務清單：tasks.yaml
   - 依賴圖：dependency-graph.md
   - 執行計劃：execution-plan.md
```

### 錯誤輸出

```
❌ 任務分解失敗：{feature-id}

🔍 錯誤位置：
   階段：{stage}
   視角：{perspective}

❓ 錯誤原因：
   {error-description}

💡 建議步驟：
   1. {action-1}
   2. {action-2}
   3. {action-3}

📖 相關文檔：
   docs/troubleshooting/E-TSK-{code}.md
```

## 文檔結構

```
skills/tasks/
├── 00-quickstart/
│   └── _base/
│       └── usage.md         # 3 分鐘入門
├── docs/
│   ├── concepts.md          # 核心概念
│   ├── schema.md            # Schema 說明
│   ├── perspectives.md      # 視角說明
│   └── integration.md       # 整合指南
└── examples/
    ├── simple-feature.yaml  # 簡單範例
    ├── complex-feature.yaml # 複雜範例
    └── with-dependencies.yaml # 依賴範例
```

## 開發者體驗優化

### 漸進式披露

1. **初學者**：使用預設值，無需任何 flag
2. **進階用戶**：使用 `--quick` / `--deep` 調整
3. **專家用戶**：使用 `--perspectives` 自訂

### 錯誤訊息設計原則

1. **位置明確**：指出錯誤發生在哪個視角
2. **原因清晰**：說明為什麼失敗
3. **步驟可行**：提供具體的修復步驟
4. **文檔連結**：提供詳細排錯指南

### 快速開始體驗

```bash
# 最簡使用（推薦初學者）
/multi-tasks user-auth

# 標準使用
/multi-tasks --from-plan user-auth

# 快速迭代
/multi-tasks --quick user-auth
```

## 與其他 Skill 的一致性

| 元素 | TASKS | PLAN | RESEARCH |
|------|-------|------|----------|
| 觸發詞 | /multi-tasks | /multi-plan | /multi-research |
| --quick | 2 視角 | 2 視角 | 2 視角 |
| --deep | 6 視角 | 6 視角 | 6 視角 |
| 輸出格式 | 統一摘要風格 | 統一摘要風格 | 統一摘要風格 |
| Memory 路徑 | .claude/memory/tasks/ | .claude/memory/plans/ | .claude/memory/research/ |

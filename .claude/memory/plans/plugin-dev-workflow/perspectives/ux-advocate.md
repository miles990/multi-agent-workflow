# UX 倡導者報告

> plugin-dev Skill 使用者體驗與介面設計

**視角**：UX 倡導者
**日期**：2026-02-01
**階段**：PLAN

## 執行摘要

基於認知研究發現（記憶負擔中等、學習曲線 2-5 天），本報告設計以**降低認知負擔**為核心目標的使用者介面。

**設計原則**：
1. 統一入口，減少記憶負擔
2. 漸進複雜度，簡單預設值
3. 視覺化回饋，即時狀態
4. 友善錯誤，引導修復
5. 新手友好，快速上手

## 命令介面設計

### 命令結構

```
/plugin-dev <command> [options]

Commands:
  sync        同步到快取
  watch       監控模式
  validate    驗證結構
  status      查看狀態
  version     版本管理
  release     發布流程
  help        顯示幫助
```

### 命令命名原則

| 原則 | 範例 | 說明 |
|------|------|------|
| 動詞優先 | sync, watch, validate | 清晰表達動作 |
| 簡短 | 1-2 音節 | 易於記憶和輸入 |
| 一致性 | 全部小寫 | 無需思考大小寫 |
| 無歧義 | release (非 publish) | 避免混淆 |

### 參數設計

**通用參數**：
```
--help, -h      顯示命令幫助
--verbose, -v   詳細輸出
--dry-run       預覽模式
--yes, -y       跳過確認
--json          JSON 格式輸出
```

**設計原則**：
- 長參數有短參數別名
- 常用操作有簡短形式
- 預設值是安全操作

### 輸出格式

**成功輸出**：
```
✓ 同步完成
  新增: 3 個檔案
  修改: 2 個檔案
  刪除: 0 個檔案
  耗時: 1.2s
```

**警告輸出**：
```
⚠ 發現潛在問題
  - plugin.json 缺少 author 欄位
  - marketplace.json 版本與 plugin.json 不一致

💡 建議: 執行 /plugin-dev validate --fix 自動修復
```

**錯誤輸出**：
```
✗ 同步失敗
  錯誤: 快取目錄不可寫入
  路徑: ~/.claude/plugins/cache/...

💡 修復建議:
  1. 檢查目錄權限: ls -la ~/.claude/plugins/
  2. 嘗試強制同步: /plugin-dev sync --force
  3. 查看詳細日誌: /plugin-dev status --verbose
```

## 視覺化設計

### Watch 模式狀態顯示

```
┌─ Plugin Development Mode ────────────────────────────┐
│                                                       │
│  專案: multi-agent-workflow                          │
│  版本: 2.4.0                                         │
│                                                       │
│  來源: /Users/user/Workspace/multi-agent-workflow   │
│  快取: ~/.claude/plugins/cache/...                  │
│                                                       │
│  狀態: 👁️  監控中 (fswatch)                          │
│  防抖動: 500ms                                       │
│                                                       │
├──────────────────────────────────────────────────────┤
│  最近同步:                                           │
│  14:30:05  ✓ 2 個檔案已同步 (120ms)                 │
│            • skills/plugin-dev/SKILL.md             │
│            • shared/plugin/config.yaml              │
│                                                       │
│  14:28:12  ✓ 1 個檔案已同步 (80ms)                  │
│            • cli/plugin/dev.py                      │
│                                                       │
├──────────────────────────────────────────────────────┤
│  按 Ctrl+C 停止監控                                  │
└──────────────────────────────────────────────────────┘
```

### Release 進度指示

```
🚀 發布 v2.4.1 (patch)

  [1/9] ✓ 驗證結構              完成
  [2/9] ✓ 執行測試              73/73 通過
  [3/9] ✓ 檢查 Git 狀態         無未提交變更
  [4/9] ✓ 升級版本              2.4.0 → 2.4.1
  [5/9] ◐ 生成變更日誌          處理中...
  [6/9] ○ Git commit            等待中
  [7/9] ○ Git tag               等待中
  [8/9] ○ Git push              等待中
  [9/9] ○ 完成                  等待中

  進度: ████████████░░░░░░░░ 55%

  按 Ctrl+C 中斷（可用 --resume 恢復）
```

### Status 輸出格式

```
📦 Plugin 狀態

  名稱: multi-agent-workflow
  版本: 2.4.0
  作者: user

快取狀態:
  路徑: ~/.claude/plugins/cache/multi-agent-workflow/2.4.0/
  狀態: ✓ 有效
  最後同步: 5 分鐘前
  檔案數: 156
  大小: 2.3 MB

Skills:
  ✓ orchestrate (v3.4.0)
  ✓ research (v3.2.0)
  ✓ plan (v3.2.0)
  ✓ tasks (v3.1.0)
  ✓ implement (v3.1.0)
  ✓ review (v3.1.0)
  ✓ verify (v3.1.0)
  ✓ status (v2.0.0)

最近發布:
  v2.4.0  2026-01-30  feat: 新增 plugin-dev Skill
  v2.3.2  2026-01-25  fix: git_lib 整合修復
  v2.3.1  2026-01-20  fix: 版本一致性檢查
```

## 新手體驗

### 首次使用引導

```
/plugin-dev

歡迎使用 plugin-dev Skill！

快速開始:
  /plugin-dev status     查看當前狀態
  /plugin-dev sync       同步到 Claude Code 快取
  /plugin-dev watch      啟動開發監控模式

發布:
  /plugin-dev release patch   發布 patch 版本
  /plugin-dev release minor   發布 minor 版本

幫助:
  /plugin-dev help           完整命令列表
  /plugin-dev <cmd> --help   特定命令幫助

📖 完整文檔: .claude/memory/plans/plugin-dev-workflow/
```

### 互動式發布嚮導

```
/plugin-dev release --interactive

🚀 發布嚮導

目前版本: 2.4.0

請選擇發布類型:
  [1] Patch (2.4.1) - Bug 修復、小改進
  [2] Minor (2.5.0) - 新功能、向後相容
  [3] Major (3.0.0) - 破壞性變更

> 1

檢測到以下變更:
  • fix(cache): 修復同步中斷問題
  • feat(watch): 新增防抖動配置

這些變更符合 Patch 版本嗎? [Y/n] y

準備執行:
  1. 驗證 Plugin 結構
  2. 執行測試套件
  3. 升級版本: 2.4.0 → 2.4.1
  4. 生成變更日誌
  5. 建立 Git commit 和 tag
  6. 推送到遠端

確認發布? [y/N] y
```

### 常見問題快速解答

```
/plugin-dev help faq

常見問題:

Q: 修改後 Claude Code 沒有更新？
A: 確保執行了同步：
   /plugin-dev sync
   然後重啟 Claude Code 或重新載入 Plugin

Q: 快取位置在哪裡？
A: 執行 /plugin-dev status 查看完整路徑
   通常在 ~/.claude/plugins/cache/

Q: 如何回復發布中斷？
A: 使用 --resume 參數：
   /plugin-dev release --resume

Q: Watch 模式不起作用？
A: 檢查監控工具是否安裝：
   - macOS: brew install fswatch
   - Linux: apt install inotify-tools
```

## 錯誤處理

### 錯誤分類

| 類型 | 符號 | 說明 |
|------|------|------|
| 致命錯誤 | ✗ | 無法繼續，需要修復 |
| 警告 | ⚠ | 可繼續，但建議處理 |
| 提示 | 💡 | 優化建議 |
| 資訊 | ℹ | 補充說明 |

### 常見錯誤與修復

**快取不存在**：
```
✗ 快取目錄不存在

  期望路徑: ~/.claude/plugins/cache/multi-agent-workflow/2.4.0/

💡 修復步驟:
  1. 這可能是首次使用，執行同步:
     /plugin-dev sync

  2. 如果問題持續，檢查 Claude Code 是否正確安裝
```

**版本不一致**：
```
⚠ 版本不一致

  plugin.json:      2.4.0
  marketplace.json: 2.3.0
  CLAUDE.md:        2.4.0

💡 修復方法:
  /plugin-dev validate --fix

  或手動編輯 marketplace.json 更新版本
```

**Git 衝突**：
```
✗ Git 狀態異常

  有未提交的變更:
    M cli/plugin/dev.py
    ? .plugin-dev/temp.json

💡 修復步驟:
  1. 提交或暫存變更:
     git add -A && git commit -m "wip: save changes"

  2. 或使用 stash:
     git stash

  3. 然後重新執行發布
```

### 錯誤恢復流程

```
發布在步驟 5 (CHANGELOG) 失敗

  ✗ 生成變更日誌失敗
    錯誤: git log 無法執行

已保存的進度:
  ✓ 步驟 1-4 已完成
  • 版本已升級到 2.4.1
  • plugin.json 已更新

恢復選項:
  [1] 重試: /plugin-dev release --resume
  [2] 回滾: /plugin-dev release --rollback
  [3] 手動修復後繼續

進度已保存到: .plugin-dev/release-progress.json
```

## 文檔設計

### usage.md 結構

```markdown
# plugin-dev Skill 快速開始

## 30 秒上手

# 1. 同步到快取
/plugin-dev sync

# 2. 驗證結構
/plugin-dev validate

# 3. 查看狀態
/plugin-dev status

## 開發模式

# 啟動監控（檔案變更自動同步）
/plugin-dev watch

## 發布新版本

# 發布 patch 版本
/plugin-dev release patch

# 預覽發布（不實際執行）
/plugin-dev release minor --dry-run

## 更多幫助

/plugin-dev help
/plugin-dev <command> --help
```

### 命令參考卡片

```
┌─────────────────────────────────────────────────────┐
│            plugin-dev 命令參考卡                    │
├─────────────────────────────────────────────────────┤
│                                                      │
│  同步     /plugin-dev sync [--force] [--dry-run]   │
│  監控     /plugin-dev watch [--debounce N]          │
│  驗證     /plugin-dev validate [--fix]              │
│  狀態     /plugin-dev status [--json]               │
│  版本     /plugin-dev version [bump LEVEL]          │
│  發布     /plugin-dev release [LEVEL] [--resume]    │
│                                                      │
│  LEVEL: patch | minor | major                       │
│                                                      │
│  通用:   --help  --verbose  --dry-run  --yes       │
│                                                      │
└─────────────────────────────────────────────────────┘
```

## 設計優先順序

### P0: 必須實現

1. 統一命令入口 `/plugin-dev`
2. 清晰的成功/失敗輸出
3. 基本幫助資訊

### P1: 應該實現

4. Watch 模式狀態顯示
5. Release 進度指示
6. 錯誤修復建議

### P2: 可以實現

7. 互動式發布嚮導
8. 彩色輸出
9. 命令參考卡片

## 驗收標準

| 指標 | 目標 |
|------|------|
| 新手上手時間 | < 30 分鐘 |
| 常用命令記憶 | < 3 個命令 |
| 錯誤可理解性 | > 90% 有修復建議 |
| 幫助覆蓋率 | 100% 命令有 --help |

## 總結

**核心 UX 改進**：
1. 從 6 個獨立腳本 → 1 個統一入口
2. 從文字輸出 → 視覺化狀態
3. 從錯誤碼 → 友善修復指南
4. 從命令記憶 → 互動式引導

**預期效果**：
- 記憶負擔降低 60%
- 學習曲線從 2-5 天 → 30 分鐘基本使用
- 錯誤恢復時間減少 50%

---

*由 UX 倡導者視角產出*

# multi-agent-workflow 優化實作計劃

**計劃版本**: 1.0  
**制定日期**: 2026-02-01  
**目標版本**: 2.4.0  
**預估總工期**: 8-10 天

---

## 1. 執行摘要

### 1.1 計劃目標

基於架構分析報告（總體評分 79/100），本計劃聚焦於三個高投資回報率（ROI）的優化方向：

1. **結構規範化** - 建立 Skill 腳手架和驗證工具，確保結構一致性
2. **視角系統集中化** - 建立視角目錄，消除重複定義
3. **配置系統優化** - 建立配置索引，減少配置分散

### 1.2 預期成果

| 指標 | 當前 | 目標 | 改善 |
|------|------|------|------|
| 新 Skill 創建時間 | 120 分鐘 | 10 分鐘 | -92% |
| 視角定義重複度 | 60% | 0% | -100% |
| 配置檔案查找時間 | 15 分鐘 | 2 分鐘 | -87% |
| 結構一致性 | 手動維護 | 自動驗證 | 100% 保證 |
| 整體可維護性評分 | 70/100 | 85/100 | +15 分 |

### 1.3 不包含範圍（Out of Scope）

以下項目不在本次優化範圍，將在後續版本處理：

- ❌ 混合架構重構（Python CLI 重新定位）
- ❌ 測試覆蓋率提升
- ❌ 階段系統可擴展化
- ❌ Agent Adapter 抽象層

---

## 2. 設計決策

### 2.1 核心設計原則

#### DRY（Don't Repeat Yourself）

**應用場景**:
- **視角定義**: 單一視角目錄，所有 Skills 引用而非重複定義
- **Skill 結構**: 模板驅動生成，避免手動複製

**實作方式**:
```yaml
# Bad: 每個 skill 重複定義
skills/research/01-perspectives/default-perspectives.md
skills/plan/01-perspectives/default-perspectives.md

# Good: 集中定義，Skills 引用
shared/perspectives/catalog.yaml  # 單一來源
skills/research/SKILL.md          # 引用: @shared/perspectives/catalog.yaml
```

#### SOLID 原則應用

**單一職責（SRP）**:
- `create-skill.sh` - 只負責生成結構
- `validate-skills.sh` - 只負責驗證結構
- `catalog.yaml` - 只負責視角定義

**開放封閉（OCP）**:
- 新增視角：修改 `catalog.yaml`，不需改 Skills
- 新增 Skill：使用腳手架工具，不需手動複製

**介面隔離（ISP）**:
- Skills 只引用需要的視角，不強制載入全部
- 配置索引提供分類，按需查找

### 2.2 技術選型

| 決策點 | 選項 A | 選項 B | 選擇 | 理由 |
|--------|--------|--------|------|------|
| 腳手架工具語言 | Shell | Python | **Shell** | 零依賴、與現有工具一致 |
| 視角目錄格式 | YAML | JSON | **YAML** | 與現有配置一致、易讀 |
| 驗證工具語言 | Shell | Python | **Shell** | 輕量、快速、CI 友好 |
| 配置索引格式 | YAML | Markdown | **YAML** | 結構化、易解析 |

---

## 3. Phase 1: 結構規範化

**目標**: 建立 Skill 腳手架工具和驗證系統  
**工期**: 3 天  
**優先級**: P0（最高）

### 3.1 Task 1.1: 定義 Skill 標準結構

**工期**: 0.5 天

**可交付成果**:
```
shared/skill-structure/
├── STANDARD.md           # 標準結構文檔
├── CLAUDE.md            # 自動載入說明
└── templates/
    ├── SKILL.md.template
    ├── quickstart.md.template
    └── perspectives.md.template
```

**STANDARD.md 內容大綱**:
```markdown
# Skill 標準結構規範

## 必須檔案
1. SKILL.md - 包含 frontmatter (name, description, version)
2. 00-quickstart/_base/usage.md - 快速開始指南
3. 01-perspectives/_base/default-perspectives.md - 預設視角清單

## 建議檔案
- config/ - 特定配置
- templates/ - Prompt 模板

## 可選檔案
- examples/ - 使用範例
- tests/ - 整合測試
```

**驗收標準**:
- [x] STANDARD.md 包含清晰的必須/建議/可選檔案說明
- [x] 模板檔案包含所有必要段落和 frontmatter
- [x] CLAUDE.md 提供自動載入說明

### 3.2 Task 1.2: 建立 Skill 腳手架工具

**工期**: 1 天

**功能需求**:

1. **互動式建立**
```bash
$ ./scripts/create-skill.sh

=== Skill 腳手架工具 ===

Skill 名稱 (kebab-case): my-new-skill
Skill 描述: 我的新 Skill
作者: Your Name

生成結構中...
✓ skills/my-new-skill/SKILL.md
✓ skills/my-new-skill/00-quickstart/_base/usage.md
✓ skills/my-new-skill/01-perspectives/_base/default-perspectives.md

完成！下一步：
1. 編輯 SKILL.md 定義執行流程
2. 配置 01-perspectives/default-perspectives.md 選擇視角
3. 執行驗證：./scripts/validate-skills.sh my-new-skill
```

2. **非互動模式**（CI 使用）
```bash
$ ./scripts/create-skill.sh --name my-skill --desc "描述" --author "Name" --non-interactive
```

**驗收標準**:
- [x] 支援互動式和非互動模式
- [x] 驗證 Skill 名稱格式（kebab-case）
- [x] 生成所有必須檔案和目錄
- [x] 模板變數正確替換
- [x] 檢查 Skill 不存在（避免覆蓋）
- [x] 提供清晰的下一步指引

### 3.3 Task 1.3: 建立結構驗證工具

**工期**: 1 天

**功能需求**:

1. **全部 Skills 驗證**
```bash
$ ./scripts/validate-skills.sh

驗證所有 Skills...
✓ research - 結構正確
✓ plan - 結構正確
✗ implement - 缺少 00-quickstart/_base/usage.md
✓ review - 結構正確

結果: 3/4 通過
```

2. **單一 Skill 驗證**
```bash
$ ./scripts/validate-skills.sh research
✓ research - 結構正確
  - SKILL.md: frontmatter 完整
  - 00-quickstart: 存在
  - 01-perspectives: 存在
```

**檢查項目**:

| 檢查項 | 必須 | 檢查內容 |
|--------|------|---------|
| SKILL.md 存在 | ✅ | 檔案存在 |
| Frontmatter 完整 | ✅ | name, description, version 欄位 |
| 標準段落存在 | ✅ | Quickstart, Phases, Error Handling |
| 00-quickstart 存在 | ✅ | 00-quickstart/_base/usage.md |
| 01-perspectives 存在 | ✅ | 01-perspectives/_base/*.md |
| Shared 模組引用 | ⚠️ | 至少引用一個 @shared 模組 |

**驗收標準**:
- [x] 檢查所有必須檔案和目錄
- [x] 驗證 frontmatter 完整性
- [x] 檢查標準段落存在
- [x] 支援全部驗證和單一驗證
- [x] CI 模式下錯誤返回非零退出碼
- [x] 提供清晰的錯誤訊息和修復指引

### 3.4 Task 1.4: 文檔和整合

**工期**: 0.5 天

**可交付成果**:

1. **更新 README.md**

新增章節：
```markdown
## 建立新 Skill

使用腳手架工具快速建立：

```bash
./scripts/create-skill.sh
```

工具會引導您輸入 Skill 名稱、描述、作者等資訊。

## 驗證 Skill 結構

```bash
# 驗證所有 Skills
./scripts/validate-skills.sh

# 驗證特定 Skill
./scripts/validate-skills.sh my-skill
```

詳細規範請參考 `shared/skill-structure/STANDARD.md`。
```

2. **整合到 CI**（如果有）

```yaml
# .github/workflows/ci.yml
- name: Validate Skills Structure
  run: ./scripts/validate-skills.sh --ci
```

**驗收標準**:
- [x] README.md 包含建立和驗證說明
- [x] 如有 CI，整合驗證步驟
- [x] 現有 Skills 通過驗證或已修復

### 3.5 Phase 1 驗收標準

**功能驗收**:
- [x] 使用 `create-skill.sh` 可在 10 分鐘內建立新 Skill
- [x] 生成的 Skill 自動通過 `validate-skills.sh` 驗證
- [x] 所有現有 Skills 通過驗證
- [x] 文檔完整，開發者可獨立操作

**品質驗收**:
- [x] 腳手架工具有錯誤處理（名稱衝突、格式錯誤）
- [x] 驗證工具提供清晰的錯誤訊息
- [x] 模板檔案包含註解和範例
- [x] 符合 Shell 最佳實踐（set -euo pipefail）

---

## 4. Phase 2: 視角系統集中化

**目標**: 建立視角目錄（Perspective Catalog）  
**工期**: 3 天  
**優先級**: P0（最高）

### 4.1 Task 2.1: 設計視角目錄結構

**工期**: 0.5 天

**可交付成果**:
```
shared/perspectives/
├── catalog.yaml          # 視角目錄（新）
├── CLAUDE.md            # 更新使用說明
└── extended/            # 擴展視角（現有）
    ├── security-auditor.md
    └── ...
```

**catalog.yaml 結構設計**:

```yaml
# shared/perspectives/catalog.yaml
version: "1.0.0"
description: "統一視角定義目錄"

# 視角分類
categories:
  core:
    description: "核心通用視角"
  domain:
    description: "領域專業視角"
  quality:
    description: "品質保證視角"

# 視角定義
perspectives:
  architecture:
    category: core
    name: "架構分析師"
    name_en: "Architecture Analyst"
    focus: "系統結構、設計模式、技術可行性"
    model_default: sonnet
    expertise_areas:
      - "系統設計"
      - "設計模式"
      - "技術選型"
  
  cognitive:
    category: core
    name: "認知研究員"
    name_en: "Cognitive Researcher"
    focus: "方法論、思維框架、認知模型"
    model_default: sonnet

# 視角組合（Presets）
presets:
  research-default:
    description: "Research 階段預設視角組合"
    perspectives:
      - architecture
      - cognitive
      - workflow
      - industry
  
  minimal:
    description: "最小視角組合（快速執行）"
    perspectives:
      - architecture
```

**設計要點**:
1. **分類系統** - 便於組織和查找
2. **豐富元數據** - 名稱、專業領域、預設模型
3. **視角組合（Presets）** - 常用組合預定義
4. **整合模型路由** - 統一配置，減少重複

**驗收標準**:
- [x] catalog.yaml 結構清晰、易擴展
- [x] 包含所有現有視角定義
- [x] 支援分類、元數據、組合
- [x] 整合 model-routing.yaml 內容

### 4.2 Task 2.2: 提取和遷移現有視角

**工期**: 1 天

**遷移步驟**:

1. **提取視角清單**

```bash
# 掃描所有 Skills 的視角定義
for skill in skills/*/01-perspectives/_base/*.md; do
  grep -E "^\| [a-z-]+ \|" "$skill"
done | sort -u > perspectives-inventory.txt
```

2. **去重和合併**

分析重複視角，合併為通用視角。

3. **填寫 catalog.yaml**

定義完整元數據，包含 used_in 欄位追蹤使用情況。

4. **定義 Presets**

為每個階段定義預設視角組合。

**驗收標準**:
- [x] 提取所有現有視角（約 20 個）
- [x] 識別並合併重複視角
- [x] catalog.yaml 包含完整元數據
- [x] 為每個階段定義 preset

### 4.3 Task 2.3: 更新 Skills 為引用模式

**工期**: 1 天

**更新範例**:

**Before** (skills/research/01-perspectives/_base/default-perspectives.md):
```markdown
# 預設視角

| ID | 名稱 | 專注領域 |
|----|------|---------|
| architecture | 架構分析師 | 系統結構、設計模式 |
| cognitive | 認知研究員 | 方法論、思維框架 |
```

**After**:
```markdown
# 預設視角

本階段使用以下視角組合（定義於 @shared/perspectives/catalog.yaml）:

**Preset**: `research-default`

包含視角:
- `architecture` - 架構分析師（系統結構、設計模式）
- `cognitive` - 認知研究員（方法論、思維框架）
- `workflow` - 工作流專家（流程優化、自動化）
- `industry` - 產業研究員（行業趨勢、市場分析）

## 自定義視角

如需新增或替換視角，參考：
@shared/perspectives/CLAUDE.md#customizing-perspectives
```

**驗收標準**:
- [x] 所有 6 個核心 Skills 更新為引用模式
- [x] 保留原始檔案作為備份（.bak）
- [x] 文檔說明如何自定義視角
- [x] 功能測試：執行 Skills 仍正常運作

### 4.4 Task 2.4: 更新 CLAUDE.md 和工具

**工期**: 0.5 天

**可交付成果**:

1. **shared/perspectives/CLAUDE.md**

```markdown
# 視角系統使用指南

當引用 `@shared/perspectives` 時，自動載入本說明。

## 視角目錄

所有視角定義於 `catalog.yaml`，包含：
- **核心視角** (core): 通用、跨領域
- **領域視角** (domain): 專業領域
- **品質視角** (quality): QA、安全、效能

## 使用 Preset

Skills 可直接引用預定義組合：

```markdown
載入視角 Preset: @shared/perspectives/catalog.yaml (preset: research-default)
```

可用 Presets:
- `research-default` - Research 階段（4 視角）
- `plan-default` - Plan 階段（4 視角）
- `minimal` - 最小組合（1 視角）

## 自定義視角

在 Skill 的 `config/custom-perspectives.yaml` 中覆蓋：

```yaml
perspectives:
  - ref: architecture      # 引用 catalog 視角
  - ref: security-auditor
  - custom:               # 完全自定義
      id: my-perspective
      name: "我的專業視角"
      focus: "特定分析領域"
      model: sonnet
```

## 視角查詢工具

```bash
# 列出所有視角
./scripts/list-perspectives.sh

# 查詢特定視角
./scripts/list-perspectives.sh architecture

# 顯示 Preset 內容
./scripts/list-perspectives.sh --preset research-default
```
```

2. **scripts/list-perspectives.sh**

提供視角查詢工具，支援列出所有視角、查詢特定視角、顯示 Preset 內容。

**驗收標準**:
- [x] CLAUDE.md 提供完整使用說明
- [x] list-perspectives.sh 可列出所有視角
- [x] 支援查詢 Preset 內容
- [x] 文檔包含新增視角範例

### 4.5 Phase 2 驗收標準

**功能驗收**:
- [x] catalog.yaml 包含所有視角定義（約 20 個）
- [x] 6 個核心 Skills 改為引用模式
- [x] Presets 定義完整（至少 4 個）
- [x] 視角查詢工具可用

**品質驗收**:
- [x] 視角定義無重複
- [x] 元數據完整（名稱、專注領域、模型）
- [x] 相容性：Skills 功能不受影響
- [x] 文檔清晰、易理解

---

## 5. Phase 3: 配置系統優化

**目標**: 建立配置索引，減少配置分散  
**工期**: 2 天  
**優先級**: P1（高）

### 5.1 Task 3.1: 分析現有配置檔案

**工期**: 0.5 天

**可交付成果**: 配置清單和分類方案

**分析步驟**:

1. **掃描所有配置檔案**

```bash
find shared -name "*.yaml" -o -name "*.yml" | sort
```

2. **分類配置檔案**

| 分類 | 配置檔案 | 用途 |
|------|---------|------|
| **execution** | execution-profiles.yaml | 執行模式 |
| | model-routing.yaml | 模型路由 |
| **quality** | gates.yaml | 品質閘門 |
| | tdd-enforcement.yaml | TDD 強制 |
| **metrics** | schema.yaml | 指標定義 |
| | thresholds.yaml | 閾值配置 |

3. **識別重複配置**

記錄重複配置項，為後續優化提供依據。

**驗收標準**:
- [x] 完整配置檔案清單（約 20 個）
- [x] 分類方案明確（5-7 個分類）
- [x] 重複配置已識別

### 5.2 Task 3.2: 建立配置索引

**工期**: 0.5 天

**可交付成果**:
```
shared/config/
├── INDEX.yaml           # 配置索引（新）
├── CLAUDE.md           # 更新說明
└── ... (現有配置檔案)
```

**INDEX.yaml 結構**:

```yaml
# shared/config/INDEX.yaml
version: "1.0.0"
description: "配置檔案索引和導航"

# 配置分類
categories:
  execution:
    description: "執行模式、模型路由、上下文管理"
    icon: "⚙️"
    configs:
      - file: shared/config/execution-profiles.yaml
        purpose: "定義 default/express/quality 執行模式"
        used_by: [orchestrate, all-skills]
      
      - file: shared/config/model-routing.yaml
        purpose: "每個階段和視角的模型映射"
        used_by: [map-phase, all-skills]
  
  quality:
    description: "品質閘門、TDD 強制、評分標準"
    icon: "✅"
    configs:
      - file: shared/quality/gates.yaml
        purpose: "階段品質閘門定義和檢查規則"
        used_by: [orchestrate, quality-gate.sh]
  
  metrics:
    description: "指標收集、閾值、報告"
    icon: "📊"
    configs:
      - file: shared/metrics/schema.yaml
        purpose: "指標定義（名稱、類型、單位）"
        used_by: [collect-metrics.sh]

# 配置查找快速參考
quick_reference:
  "如何改變執行模式？": shared/config/execution-profiles.yaml
  "如何為視角指定模型？": shared/config/model-routing.yaml
  "如何調整品質閘門？": shared/quality/gates.yaml

# 配置依賴關係
dependencies:
  execution-profiles.yaml:
    depends_on: [model-routing.yaml]
    description: "執行模式會引用 model-routing 中的模型定義"
```

**驗收標準**:
- [x] 分類清晰、易導航
- [x] 每個配置有用途說明
- [x] 包含快速參考（常見問題）
- [x] 記錄配置依賴關係

### 5.3 Task 3.3: 合併重疊配置

**工期**: 0.5 天

**目標**: 減少配置重複，提升一致性

**方案**: 保持分離，強化引用關係

**實作**:
1. **保持配置分離**（符合 SRP）
2. **在 INDEX.yaml 中記錄引用關係**
3. **提供查詢工具簡化使用**

**scripts/get-config.sh**

```bash
#!/bin/bash
# scripts/get-config.sh
# 查詢配置值（支援跨檔案引用）

usage() {
  echo "用法: $0 <config-path>"
  echo "範例: $0 execution.default.perspectives_per_stage"
}

# 根據路徑查詢配置
get_value() {
  local path="$1"
  local category=$(echo "$path" | cut -d. -f1)
  
  case "$category" in
    execution)
      file="shared/config/execution-profiles.yaml"
      ;;
    quality)
      file="shared/quality/gates.yaml"
      ;;
    *)
      echo "未知配置分類: $category"
      return 1
      ;;
  esac
  
  # 使用 yq 或 grep 查詢
  if command -v yq &> /dev/null; then
    yq eval ".$path" "$file"
  else
    grep "$(echo $path | cut -d. -f2-):" "$file"
  fi
}

if [[ $# -eq 0 ]]; then
  usage
  exit 1
fi

get_value "$1"
```

**驗收標準**:
- [x] 配置檔案保持分離
- [x] INDEX.yaml 記錄引用關係
- [x] 提供配置查詢工具
- [x] 文檔說明配置依賴

### 5.4 Task 3.4: 更新文檔

**工期**: 0.5 天

**可交付成果**:

1. **shared/config/CLAUDE.md**

```markdown
# 配置系統使用指南

當引用 `@shared/config` 時，自動載入本說明。

## 配置索引

所有配置檔案索引於 `INDEX.yaml`，按分類組織：
- ⚙️ **execution** - 執行模式、模型路由
- ✅ **quality** - 品質閘門、TDD 強制
- 📊 **metrics** - 指標定義、閾值

## 快速查找

**常見問題**:
- 如何改變執行模式？ → `execution-profiles.yaml`
- 如何為視角指定模型？ → `model-routing.yaml`
- 如何調整品質閘門？ → `shared/quality/gates.yaml`

完整索引參考: `@shared/config/INDEX.yaml`

## 配置查詢工具

```bash
# 查詢配置值
./scripts/get-config.sh execution.default.perspectives_per_stage
# 輸出: 4
```
```

2. **更新 README.md**

新增配置系統章節，說明如何查找和修改配置。

**驗收標準**:
- [x] CLAUDE.md 提供清晰導航
- [x] README.md 包含配置查找說明
- [x] 文檔包含常見問題快速參考

### 5.5 Phase 3 驗收標準

**功能驗收**:
- [x] INDEX.yaml 包含所有配置檔案（約 20 個）
- [x] 分類清晰、易導航
- [x] 配置查詢工具可用
- [x] 文檔完整

**品質驗收**:
- [x] 配置職責清晰（未過度合併）
- [x] 引用關係明確記錄
- [x] 快速參考準確

**非功能驗收**:
- [x] 配置查找時間減少 85%（15 分鐘 → 2 分鐘）
- [x] 新貢獻者可快速定位配置

---

## 6. 驗收標準

### 6.1 整體驗收標準

**功能完整性**:
- [x] Phase 1: Skill 腳手架和驗證工具可用
- [x] Phase 2: 視角目錄建立，Skills 改為引用
- [x] Phase 3: 配置索引建立，查詢工具可用

**品質指標**:
- [x] 新 Skill 創建時間 < 10 分鐘
- [x] 視角定義重複度 = 0%
- [x] 配置查找時間 < 2 分鐘
- [x] 結構驗證自動化（CI 整合）

**文檔完整性**:
- [x] 每個工具有使用說明
- [x] README.md 更新
- [x] CLAUDE.md 提供自動載入指南
- [x] 包含範例和最佳實踐

**相容性**:
- [x] 現有 Skills 功能不受影響
- [x] 向後相容（支援自定義）
- [x] 無破壞性變更

### 6.2 測試計劃

#### 整合測試

**測試場景**:

1. **建立新 Skill 完整流程**
   ```bash
   ./scripts/create-skill.sh --name test-skill --non-interactive
   ./scripts/validate-skills.sh test-skill
   # 預期: ✓ 結構正確
   ```

2. **視角引用功能**
   - 驗證 Skills 可正確引用 catalog.yaml
   - 執行 research skill，檢查視角載入

3. **配置查詢功能**
   ```bash
   ./scripts/get-config.sh execution.default.perspectives_per_stage
   # 預期: 4
   ```

#### 回歸測試

**測試內容**:
- 所有現有 Skills 仍正常執行
- 現有工作流（orchestrate）功能不受影響
- 現有配置檔案仍可正常載入

### 6.3 驗收檢查清單

**Phase 1: 結構規範化**
- [ ] create-skill.sh 可建立標準結構
- [ ] validate-skills.sh 可驗證結構
- [ ] 所有現有 Skills 通過驗證
- [ ] README.md 包含建立說明
- [ ] CI 整合（如適用）

**Phase 2: 視角系統集中化**
- [ ] catalog.yaml 包含所有視角
- [ ] Presets 定義完整
- [ ] Skills 改為引用模式
- [ ] list-perspectives.sh 可用
- [ ] CLAUDE.md 提供使用指南

**Phase 3: 配置系統優化**
- [ ] INDEX.yaml 包含所有配置
- [ ] 分類清晰、易導航
- [ ] get-config.sh 可用
- [ ] 配置依賴關係記錄
- [ ] CLAUDE.md 提供查找指南

**整體驗收**
- [ ] 所有工具執行無錯誤
- [ ] 文檔完整、清晰
- [ ] 回歸測試通過
- [ ] 效能指標達標

---

## 7. 風險評估

### 7.1 技術風險

| 風險 | 概率 | 影響 | 緩解措施 |
|------|------|------|---------|
| **Shell 腳本相容性問題**（macOS vs Linux） | 中 | 中 | 使用 POSIX 標準語法，避免 GNU 特定功能 |
| **YAML 解析複雜度**（無 yq 環境） | 中 | 低 | 提供 Fallback（grep-based 解析） |
| **視角遷移導致功能異常** | 低 | 高 | 保留備份檔案（.bak），完整回歸測試 |
| **配置引用關係錯誤** | 低 | 中 | INDEX.yaml 仔細驗證，提供查詢工具測試 |

### 7.2 業務風險

| 風險 | 概率 | 影響 | 緩解措施 |
|------|------|------|---------|
| **開發者學習曲線** | 中 | 低 | 提供詳細文檔、範例、快速開始指南 |
| **破壞現有工作流** | 低 | 高 | 完整回歸測試、向後相容設計 |
| **工具採用率低** | 中 | 中 | 整合到 CI，在文檔中推廣使用 |

### 7.3 排程風險

| 風險 | 概率 | 影響 | 緩解措施 |
|------|------|------|---------|
| **工期低估**（視角遷移複雜） | 中 | 中 | 預留 20% 緩衝時間（8-10 天彈性） |
| **測試覆蓋不足** | 中 | 中 | 優先整合測試，確保核心功能 |
| **文檔編寫耗時** | 低 | 低 | 使用模板、複用現有文檔結構 |

### 7.4 風險應對策略

**高優先級風險**:

1. **視角遷移導致功能異常**
   - **預防**: 完整備份、階段式遷移（先 1 個 Skill 測試）
   - **檢測**: 回歸測試、手動功能測試
   - **恢復**: 保留 .bak 檔案，快速回滾

2. **Shell 腳本相容性問題**
   - **預防**: 使用 shellcheck 靜態檢查
   - **測試**: 在 macOS 和 Linux 環境測試
   - **Fallback**: 提供 Python 替代實作（如需要）

**中優先級風險**:

3. **開發者學習曲線**
   - **緩解**: 提供互動式工具（減少記憶負擔）
   - **文檔**: 快速開始指南、常見問題
   - **支援**: 在 README 提供聯絡方式

---

## 附錄 A: 工具快速參考

### 建立新 Skill

```bash
# 互動式
./scripts/create-skill.sh

# 非互動式
./scripts/create-skill.sh --name my-skill --desc "描述" --author "Name" --non-interactive
```

### 驗證 Skill 結構

```bash
# 驗證所有 Skills
./scripts/validate-skills.sh

# 驗證特定 Skill
./scripts/validate-skills.sh my-skill

# CI 模式（嚴格）
./scripts/validate-skills.sh --ci
```

### 查詢視角

```bash
# 列出所有視角
./scripts/list-perspectives.sh

# 查詢特定視角
./scripts/list-perspectives.sh architecture

# 顯示 Preset
./scripts/list-perspectives.sh --preset research-default
```

### 查詢配置

```bash
# 查詢配置值
./scripts/get-config.sh execution.default.perspectives_per_stage

# 查詢品質閘門
./scripts/get-config.sh quality.gates.RESEARCH
```

---

## 附錄 B: 設計決策記錄（ADR）

### ADR-001: 使用 Shell 腳本而非 Python

**背景**: 需要建立腳手架和驗證工具

**決策**: 使用 Bash Shell 腳本

**理由**:
- ✅ 零依賴（符合專案 "零依賴" 理念）
- ✅ 與現有工具一致（shared/tools/*.sh）
- ✅ 快速、輕量
- ✅ CI 友好（無需安裝 Python 依賴）

**後果**:
- ⚠️ 需注意 macOS/Linux 相容性
- ⚠️ 複雜邏輯可能不如 Python 清晰

**緩解**: 使用 POSIX 標準語法，提供 Python Fallback（如需要）

---

### ADR-002: 視角引用採用混合式（Markdown + YAML）

**背景**: 需要讓 Skills 引用集中定義的視角

**決策**: Markdown 說明 + YAML 引用

**理由**:
- ✅ 語義清晰（Markdown 易讀）
- ✅ 結構化（YAML 易解析）
- ✅ 靈活（支援自定義覆蓋）

**替代方案**:
- ❌ 純 YAML 嵌入：缺乏說明
- ❌ 純 Markdown 引用：難以解析

**後果**:
- Skills 需要同時維護 Markdown 說明和引用路徑
- 但提升了可讀性和可維護性

---

### ADR-003: 配置檔案保持分離

**背景**: model-routing.yaml 和 execution-profiles.yaml 有重疊

**決策**: 保持分離，不合併

**理由**:
- ✅ 符合單一職責原則（SRP）
- ✅ 各配置獨立演進
- ✅ 避免檔案過大

**替代方案**:
- ❌ 合併到一個檔案：違反 SRP

**緩解**: 在 INDEX.yaml 記錄引用關係，提供查詢工具

---

## 附錄 C: 參考資料

### 相關文件

| 文件 | 路徑 | 用途 |
|------|------|------|
| 架構分析報告 | `/Users/user/Workspace/multi-agent-workflow/.claude/memory/research/architecture-analysis.md` | 本計劃基礎 |
| 專案 README | `/Users/user/Workspace/multi-agent-workflow/README.md` | 現有文檔 |
| 專案 CLAUDE.md | `/Users/user/Workspace/multi-agent-workflow/CLAUDE.md` | 專案說明 |

### 外部資源

- POSIX Shell 規範: https://pubs.opengroup.org/onlinepubs/9699919799/
- YAML 規範: https://yaml.org/spec/1.2/spec.html
- Shellcheck: https://www.shellcheck.net/

---

**計劃結束**

本計劃提供了詳細的實作步驟、驗收標準和風險評估。建議按 Phase 1 → Phase 2 → Phase 3 順序執行，每個 Phase 完成後進行階段驗收，確保品質。

預期成果：
- 新 Skill 創建時間從 120 分鐘降至 10 分鐘（-92%）
- 視角定義重複度從 60% 降至 0%（-100%）
- 配置查找時間從 15 分鐘降至 2 分鐘（-87%）
- 整體可維護性評分從 70 提升至 85（+15 分）

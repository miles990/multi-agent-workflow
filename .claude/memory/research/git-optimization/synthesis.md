# Git 使用優化研究匯總

> 多視角研究匯總報告 - multi-agent-workflow plugin Git 優化方案

**研究日期**: 2026-02-01
**研究主題**: 優化 multi-agent-workflow plugin 的 Git 使用
**設計原則**: 完美、優雅、可靠性、可維護性、可擴充性、可移植性、DRY、SOLID

---

## 執行摘要

經過四個視角（架構分析、工作流設計、業界實踐、認知科學）的深入研究，發現當前 Git 操作存在以下核心問題：

| 問題類別 | 嚴重度 | 共識度 |
|---------|--------|--------|
| **DRY 違反** | 🔴 高 | 4/4 視角一致 |
| **缺乏抽象層** | 🔴 高 | 4/4 視角一致 |
| **Hook 雙軌制混亂** | 🔴 高 | 3/4 視角提及 |
| **狀態管理分散** | 🟡 中 | 3/4 視角提及 |
| **SOLID 原則違反** | 🟡 中 | 2/4 視角詳細分析 |

**統一解決方案**：建立分層 Git 操作模組，實現統一 API。

---

## 1. 視角共識分析

### 1.1 所有視角一致認同的問題

#### 問題 1：`_get_current_workflow_id()` 重複 5 次

```python
# 65 行完全相同的代碼分散在 5 個 hook 檔案中
def _get_current_workflow_id(project_dir: str) -> str:
    workflow_dir = Path(project_dir) / ".claude" / "workflow"
    # ... 13 行相同邏輯
```

**影響**：
- 修改需同步 5 處
- Bug 修復容易遺漏
- 認知負擔高

#### 問題 2：Git 操作缺乏抽象

```python
# 所有 hook 直接呼叫 subprocess
subprocess.run(["git", "status", "--porcelain", ...])
subprocess.run(["git", "add", ...])
subprocess.run(["git", "commit", "-m", ...])
```

**影響**：
- 錯誤處理不一致
- 難以測試（需要真實 Git 環境）
- 違反 DIP 原則

#### 問題 3：Commit Message 格式分散

- `post_task.py`: `chore(task): {description}`
- `subagent_stop.py`: `{type}({memory_type}): complete {topic}`
- `workflow_hooks.py`: `{message}` (簡單格式)
- Co-Author 字串重複 16+ 次

### 1.2 統一解決方案架構

四個視角都建議類似的分層架構：

```
┌─────────────────────────────────────────────────────────────┐
│                    Hook Scripts (高層)                       │
│  post_task.py | subagent_stop.py | workflow_hooks.py        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                 WorkflowCommitFacade (外觀)                  │
│  - auto_commit_after_task()                                 │
│  - auto_commit_memory()                                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│               GitCommitService (業務邏輯)                    │
│  - commit_task_changes()                                    │
│  - commit_memory_changes()                                  │
│  - generate_commit_message()                                │
└─────────────────────────────────────────────────────────────┘
                            │
              ┌─────────────┴─────────────┐
              ▼                           ▼
┌────────────────────────┐  ┌────────────────────────┐
│    GitRepository        │  │   WorkflowContext      │
│  - has_changes()        │  │  - get_workflow_id()   │
│  - stage()              │  │  - get_commit_type()   │
│  - commit()             │  │  - load_settings()     │
└────────────────────────┘  └────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────────────┐
│                 GitExecutor (底層命令)                       │
│  - run(["git", ...]) with unified error handling           │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 視角特有洞察

### 2.1 架構視角洞察

- **模組結構建議**：建立 `scripts/git_lib/` 目錄
- **語言統一**：從 Shell + Python 混合改為純 Python
- **預估效益**：Hook 代碼減少 54%（546 → 250 行）
- **遷移計劃**：6 Phase，總計 11-17 天

### 2.2 工作流視角洞察

- **Hook 雙軌制**：templates/ 和 scripts/hooks/ 重複但行為不同
- **狀態管理**：current.json 被 8 個模組讀取、5 個模組寫入
- **建議**：使用 WorkflowState 抽象層 + 檔案鎖機制
- **Commit 時機**：
  - Task 完成 → 只 commit 程式碼
  - Subagent 結束 → 只 commit memory
  - Checkpoint → 里程碑 commit

### 2.3 業界實踐洞察

- **推薦工作流**：Trunk-Based Development
- **Python Git 庫**：pygit2（主）+ GitPython（fallback）
- **Hook 框架**：pre-commit framework
- **Worktree 最佳實踐**：
  - 視為臨時工作區
  - 任務完成後立即清理
  - 使用 `git worktree prune` 定期清理
- **衝突預防**：
  - 短期分支（< 2 天）
  - Git rerere 自動記錄解決方案

### 2.4 認知科學視角洞察

- **認知負擔分析**：
  - 當前：需理解 ~1500 行代碼
  - 重構後：~200 行（減少 87%）
- **SOLID 評分**：
  - SRP: 6/10
  - OCP: 3/10（最差）
  - LSP: 8/10
  - ISP: 5/10
  - DIP: 2/10（最差）
- **設計模式建議**：
  - Facade（高優先級）
  - Strategy（中優先級）
  - Template Method（中優先級）
  - Repository（高優先級）

---

## 3. 統一實施計劃

### Phase 1: 基礎設施建立（1-2 天）

**目標**：建立統一 Git 操作模組

**任務**：
1. 建立 `scripts/git_lib/` 目錄結構
2. 實作 `exceptions.py` - 自訂例外
3. 實作 `executor.py` - GitExecutor 底層命令執行
4. 實作 `operations.py` - GitOps 基本操作
5. 撰寫單元測試

**產出**：
```
scripts/git_lib/
├── __init__.py
├── exceptions.py      # GitExecutionError, WorkflowNotFoundError
├── executor.py        # GitExecutor 類別
└── operations.py      # GitOps 類別
```

### Phase 2: 管理層實作（2-3 天）

**目標**：建立業務邏輯抽象

**任務**：
1. 實作 `context.py` - WorkflowContext
2. 實作 `config.py` - ConfigManager
3. 實作 `commit.py` - CommitManager
4. 更新 `shared/config/commit-settings.yaml`
5. 撰寫整合測試

**產出**：
```
scripts/git_lib/
├── context.py         # WorkflowContext 類別
├── config.py          # ConfigManager 類別
└── commit.py          # CommitManager 類別
```

### Phase 3: Hook 重構（2-3 天）

**目標**：使用新模組重構現有 Hook

**任務**：
1. 建立 `facade.py` - WorkflowCommitFacade
2. 重構 `post_task.py`
3. 重構 `subagent_stop.py`
4. 整合 `templates/hooks/workflow_hooks.py` 使用委派模式
5. 執行迴歸測試

### Phase 4: Worktree 管理（3-4 天）

**目標**：實作統一 Worktree 管理

**任務**：
1. 實作 `worktree.py` - WorktreeManager
2. 整合 `shared/isolation/` 規範
3. 支援完整生命週期（create/complete/cleanup）
4. 撰寫測試

### Phase 5: Shell 遷移（2-3 天）

**目標**：用 Python 重寫 workflow-init.sh

**任務**：
1. 實作 `scripts/workflow_init.py`
2. 保留 Shell wrapper 向後相容
3. 更新相關文件
4. 測試完整工作流

### Phase 6: 文件與清理（1-2 天）

**目標**：完善文件和清理

**任務**：
1. 撰寫完整 API 文件
2. 更新 `shared/git/commit-protocol.md`
3. 更新 `README.md`
4. 更新 `CLAUDE.md`（開發經驗）
5. 移除舊的重複程式碼

---

## 4. 預期效益

### 4.1 量化效益

| 指標 | 改善 |
|------|------|
| Hook 代碼行數 | -54%（546 → 250 行）|
| 重複代碼 | -95% |
| 測試覆蓋率 | 0% → 80%+ |
| 新 hook 開發時間 | -70% |
| 新人理解時間 | -87%（5 天 → 1 天）|

### 4.2 質性效益

- **DRY 符合**：消除所有重複邏輯
- **SOLID 改善**：
  - OCP：配置驅動，無需修改代碼
  - DIP：依賴抽象介面
- **可測試性**：易於 mock，單元測試簡單
- **可維護性**：每個類別職責單一
- **可擴展性**：Strategy 模式支援新格式

---

## 5. 設計決策

### 5.1 語言選擇：Python

**決策**：統一使用 Python，移除 Shell 腳本

**理由**：
1. 已有大量 Python hook 實作
2. 複雜的 JSON/YAML 處理更方便
3. 更好的測試能力
4. 統一的錯誤處理和日誌記錄

### 5.2 Git 庫選擇：subprocess + 抽象層

**決策**：繼續使用 subprocess，但加上抽象層

**理由**：
1. 零依賴（不引入 GitPython 或 pygit2）
2. 符合 plugin 設計原則
3. 透過 GitExecutor 統一錯誤處理
4. 未來可選擇性替換為 pygit2

### 5.3 Hook 整合方式：委派模式

**決策**：templates/workflow_hooks.py 委派給 scripts/hooks/

**理由**：
1. 保持單一入口點
2. 使用完整實作
3. 向後兼容

---

## 6. 風險評估

| 風險 | 機率 | 影響 | 緩解措施 |
|------|-----|------|---------|
| 破壞現有行為 | 中 | 高 | 完整測試 + 分階段重構 |
| 遷移成本高 | 中 | 中 | 分 6 個 Phase，可暫停 |
| 學習曲線 | 低 | 低 | 提供文件 + 範例 |
| 效能下降 | 極低 | 低 | 抽象層很薄 |

---

## 7. 下一步行動

### 立即（Phase 1）

1. 建立 `scripts/git_lib/` 目錄
2. 實作 `GitExecutor` 類別
3. 實作 `GitOps` 類別
4. 撰寫基礎測試

### 短期（Phase 2-3）

1. 實作 `WorkflowContext`
2. 實作 `CommitManager`
3. 重構 hooks

### 中期（Phase 4-6）

1. WorktreeManager
2. 遷移 Shell 腳本
3. 完善文件

---

## 附錄：視角報告連結

- [架構視角](./perspectives/architecture.md)
- [工作流視角](./perspectives/workflow.md)
- [業界實踐視角](./perspectives/industry.md)
- [認知科學視角](./perspectives/cognitive.md)

---

**報告完成時間**: 2026-02-01
**視角數量**: 4
**共識度**: 高
**品質分數**: 85/100

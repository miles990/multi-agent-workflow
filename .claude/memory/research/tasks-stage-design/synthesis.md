# TASKS 階段完整設計方案

> 多視角研究匯總：6 視角並行研究 → 交叉驗證 → 統一設計

**研究日期**：2026-01-25
**視角數量**：6（深度模式）
**研究主題**：TASKS 階段設計 - 在 PLAN 與 IMPLEMENT 之間的任務分解與依賴分析階段

---

## 執行摘要

### 核心結論

TASKS 階段應作為 **獨立 Skill** 加入工作流，位於 PLAN 和 IMPLEMENT 之間，採用 **4 視角多 Agent 並行** 設計，負責將宏觀計劃分解為可執行的細粒度任務。

### 新工作流

```
RESEARCH → PLAN → TASKS → IMPLEMENT → REVIEW → VERIFY
                    ↑
                 新增階段
```

### 關鍵價值

| 面向 | 價值 |
|------|------|
| **職責分離** | PLAN 做設計，TASKS 做分解，IMPLEMENT 專注執行 |
| **並行效率** | 明確的 Wave 分組，最大化並行機會 |
| **品質保障** | 測試任務 + 風險任務內建，TDD 先行 |
| **可追溯性** | 每個任務有明確的驗收標準和依賴關係 |

---

## 1. 共識發現（6/6 視角同意）

### 1.1 TASKS 階段必要性

**共識**：PLAN 到 IMPLEMENT 之間存在抽象層級斷裂，需要 TASKS 階段彌補。

| 視角 | 支持理由 |
|------|----------|
| 架構分析師 | PLAN 產出里程碑級任務，對 Agent 太抽象 |
| 方法論專家 | 任務分解是獨立的認知活動，值得專門處理 |
| 依賴分析專家 | 需要專門階段建立 DAG 和執行順序 |
| 整合設計師 | 現有 IMPLEMENT 承擔雙重職責（分解+實作） |
| 視角設計師 | TASKS 的視角與 PLAN/IMPLEMENT 不同，應獨立 |
| 業界研究員 | 主流工具都有任務分解層（Jira Story、Linear Issue） |

### 1.2 最佳任務粒度

**共識**：10-60 分鐘可完成的任務單元

| 視角 | 粒度建議 |
|------|----------|
| 方法論專家 | 符合單次監督審查週期 |
| 視角設計師 | 對應 IMPLEMENT 的一個監督循環 |
| 業界研究員 | Claude Code TodoWrite 建議 10-20 分鐘 |

### 1.3 採用 DAG 依賴模型

**共識**：使用有向無環圖表示任務依賴，支援拓撲排序和並行識別

| 視角 | 相關建議 |
|------|----------|
| 依賴分析專家 | 三層依賴：硬依賴、軟依賴、資源依賴 |
| 業界研究員 | GitLab CI 的 DAG 實現是最佳參考 |
| 整合設計師 | Wave 分組優於純拓撲排序 |

### 1.4 YAML 作為主要格式

**共識**：tasks.yaml 作為結構化任務定義的標準格式

| 視角 | 格式建議 |
|------|----------|
| 業界研究員 | YAML 可讀性高、支援註解、結構清晰 |
| 整合設計師 | 與現有 meta.yaml 格式一致 |
| 方法論專家 | 驗收標準需要結構化表達 |

---

## 2. 架構設計

### 2.1 TASKS 階段職責

```yaml
TASKS 階段:
  核心問題: "怎麼拆解？"
  抽象層級: Tactical（戰術層）

  輸入:
    - plans/{id}/implementation-plan.md  # 技術方案
    - plans/{id}/milestones.md           # 里程碑
    - plans/{id}/risk-mitigation.md      # 風險緩解（可選）

  輸出:
    - tasks/{id}/tasks.yaml              # 任務定義（主檔案）
    - tasks/{id}/dependency-graph.md     # 依賴圖
    - tasks/{id}/execution-plan.md       # 執行計劃
    - tasks/{id}/perspectives/*.md       # 視角報告
```

### 2.2 職責邊界

| 階段 | 核心問題 | 產出 | 抽象層級 |
|------|----------|------|----------|
| **PLAN** | 怎麼設計？ | 技術方案、架構、里程碑 | Strategic |
| **TASKS** | 怎麼拆解？ | 任務清單、依賴圖、執行順序 | Tactical |
| **IMPLEMENT** | 怎麼實作？ | 程式碼、測試 | Operational |

### 2.3 數據流

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            TASKS 數據流                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  PLAN output                                                             │
│  ┌────────────────────┐                                                  │
│  │ implementation-    │                                                  │
│  │ plan.md            │──┐                                               │
│  │                    │  │                                               │
│  │ milestones.md      │──┼──→  TASKS 階段                                │
│  │                    │  │     ┌────────────────────────────────────┐   │
│  │ risk-mitigation.md │──┘     │  Phase 1: 依賴分析（先行）          │   │
│  └────────────────────┘        │      ↓                              │   │
│                                │  Phase 2: 並行任務分解              │   │
│                                │      ↓                              │   │
│                                │  Phase 3: REDUCE 整合               │   │
│                                └────────────────────────────────────┘   │
│                                        ↓                                 │
│  TASKS output                  IMPLEMENT input                           │
│  ┌────────────────────┐        ┌────────────────────┐                   │
│  │ tasks.yaml         │───────→│ 載入任務清單       │                   │
│  │ dependency-graph.md│───────→│ 按 Wave 執行       │                   │
│  │ execution-plan.md  │───────→│ 追蹤任務狀態       │                   │
│  └────────────────────┘        └────────────────────┘                   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 視角配置

### 3.1 推薦 4 視角

| ID | 名稱 | 聚焦領域 | 方法 | 產出 |
|----|------|----------|------|------|
| `dependency-analyst` | 依賴分析師 | 依賴關係、執行順序、並行機會 | deep | dependency-graph.md |
| `task-decomposer` | 任務分解師 | 實作任務、估算、驗收標準 | deep | implementation-tasks.yaml |
| `test-planner` | 測試規劃師 | 測試任務、TDD 案例、邊界條件 | deep | test-tasks.yaml |
| `risk-preventor` | 風險預防師 | 風險任務、回退點、監控任務 | deep | risk-tasks.yaml |

### 3.2 執行模式

| 模式 | 視角數 | 配置 | 適用場景 |
|------|--------|------|----------|
| `--quick` | 2 | task-decomposer + dependency-analyst | 小功能、快速迭代 |
| `--normal` | 4 | 全部 4 視角 | 標準功能開發 |
| `--deep` | 6 | 4 視角 + parallel-optimizer + doc-planner | 大型功能、重構 |

### 3.3 協作模式：分工彙整型

```
┌─────────────────────────────────────────────────────────┐
│  Phase 1: 依賴分析（先行）                               │
│  ┌──────────────────────────────────────────────────┐  │
│  │  dependency-analyst                               │  │
│  │  產出：dependency-graph.md                        │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
         ↓ 依賴圖作為輸入
┌─────────────────────────────────────────────────────────┐
│  Phase 2: 並行任務分解                                   │
│  ┌──────────────┬──────────────┬──────────────┐        │
│  │ task-        │ test-        │ risk-        │        │
│  │ decomposer   │ planner      │ preventor    │        │
│  │              │              │              │        │
│  │ T-xxx 任務   │ TEST-xxx     │ RISK-xxx     │        │
│  └──────────────┴──────────────┴──────────────┘        │
└─────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────┐
│  Phase 3: REDUCE                                         │
│  • 合併任務清單（彙整，非去重）                          │
│  • 應用依賴分析結果                                      │
│  • 生成 Wave 分組                                        │
│  • 產出 tasks.yaml                                       │
└─────────────────────────────────────────────────────────┘
```

---

## 4. tasks.yaml Schema

### 4.1 完整結構

```yaml
# .claude/memory/tasks/{feature-id}/tasks.yaml
version: "1.0.0"
metadata:
  feature_id: "{feature-id}"
  source_plan: ".claude/memory/plans/{feature-id}/implementation-plan.md"
  generated_at: "2026-01-25T10:00:00Z"
  total_tasks: 15
  estimated_duration: "8h"

# 執行配置
config:
  parallel_execution: true
  max_concurrent: 4
  pass_at_k: 3
  retry_on_failure: true

# 里程碑定義
milestones:
  - id: M1
    name: "基礎設施"
    tasks: [T-F-001, T-F-002, TEST-001]
  - id: M2
    name: "核心功能"
    depends_on: [M1]
    tasks: [T-F-003, T-F-004, TEST-002, TEST-003]

# 任務定義
tasks:
  # 功能任務（來自 task-decomposer）
  - id: T-F-001
    type: feature
    milestone: M1
    name: "實作 JWT token 生成函數"
    description: |
      建立 JWT token 生成與驗證的核心函數。
      使用 HS256 演算法，token 有效期 24 小時。

    estimate:
      duration: "30m"
      complexity: M  # XS/S/M/L/XL

    priority: P0  # P0 必做 / P1 應做 / P2 可延

    dependencies:
      blocked_by: []
      blocks: [T-F-002, T-F-003]

    acceptance_criteria:
      - "src/auth/jwt.ts 檔案存在"
      - "導出 generateToken() 和 verifyToken() 函數"
      - "npm test -- --grep 'JWT' 通過"

    files:
      create: [src/auth/jwt.ts]
      modify: []

    # 監督提示（供 IMPLEMENT 使用）
    supervision_hints:
      tdd: "先寫測試：過期 token、無效簽名"
      security: "檢查 secret 是否硬編碼"

    status: pending  # pending | in_progress | completed | failed | skipped

  # 測試任務（來自 test-planner）
  - id: TEST-001
    type: test
    milestone: M1
    name: "JWT 函數單元測試"
    related_task: T-F-001

    test_cases:
      - scenario: "生成有效 token"
        expected: "返回 JWT 字串"
      - scenario: "驗證有效 token"
        expected: "返回 payload"
      - scenario: "驗證過期 token"
        expected: "拋出 TokenExpiredError"

    priority: P0
    dependencies:
      blocked_by: []  # 測試先於實作（TDD）
      blocks: [T-F-001]

    status: pending

  # 風險任務（來自 risk-preventor）
  - id: RISK-001
    type: prevention
    milestone: M1
    name: "設定認證服務監控"
    related_risk: "R-002 認證效能問題"

    trigger: "部署後"
    steps:
      - "設定登入延遲告警 (> 2s)"
      - "設定失敗率告警 (> 5%)"

    priority: P1
    dependencies:
      blocked_by: [T-F-002]
      blocks: []

    status: pending

# 執行計劃
execution_plan:
  waves:
    - id: wave-1
      tasks: [TEST-001, T-F-001]  # 可並行
      sync_point: "M1 基礎完成"

    - id: wave-2
      tasks: [T-F-002, TEST-002]
      depends_on: [wave-1]

    - id: wave-3
      tasks: [T-F-003, T-F-004, RISK-001]
      depends_on: [wave-2]

  critical_path: [TEST-001, T-F-001, T-F-002, T-F-003]
  estimated_total: "8h"

# 統計摘要
summary:
  by_type:
    feature: 6
    test: 5
    prevention: 2
    rollback: 1
    monitoring: 1
  by_priority:
    P0: 8
    P1: 5
    P2: 2
  by_milestone:
    M1: 4
    M2: 6
    M3: 5
```

### 4.2 任務類型

| 類型前綴 | 類型 | 來源視角 | 說明 |
|----------|------|----------|------|
| `T-F-` | feature | task-decomposer | 功能實作任務 |
| `T-R-` | refactor | task-decomposer | 重構任務 |
| `TEST-` | test | test-planner | 測試任務 |
| `RISK-` | prevention/rollback/monitoring | risk-preventor | 風險預防任務 |

---

## 5. 執行流程

### 5.1 完整流程圖

```
/multi-tasks [plan-id]
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Phase 0: 計劃載入                                               │
│  • 從 PLAN 載入 implementation-plan.md                           │
│  • 載入 milestones.md 和 risk-mitigation.md                     │
│  • 驗證輸入完整性                                                │
│  <!-- METRICS: start_stage stage_id=tasks -->                   │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Phase 1: Memory 搜尋                                            │
│  • 搜尋 .claude/memory/tasks/ 相關任務定義                      │
│  • 識別可重用的任務模板                                          │
│  • 如有相關紀錄 → 詢問是否復用                                   │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Phase 2: 視角分解                                               │
│  • 根據計劃內容調整視角重點                                      │
│  • 為每個視角生成專屬 prompt                                     │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Phase 3: MAP（並行分解）                                        │
│                                                                  │
│  Step 3.1: 依賴分析（先行）                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  dependency-analyst → dependency-graph.md                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│           ↓                                                      │
│  Step 3.2: 並行任務分解                                          │
│  ┌──────────────┬──────────────┬──────────────┐                 │
│  │ task-        │ test-        │ risk-        │                 │
│  │ decomposer   │ planner      │ preventor    │                 │
│  └──────────────┴──────────────┴──────────────┘                 │
│                                                                  │
│  ⚠️ 重要：每個 Agent 完成後立即保存報告：                        │
│     Write → perspectives/{perspective_id}.md                    │
│                                                                  │
│  <!-- METRICS: record_agent 每個視角完成時記錄 -->               │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Phase 4: 同步檢查點                                             │
│  • S1: 分解進度確認（50%）                                       │
│  • S2: 任務清單驗證（80%）                                       │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Phase 5: REDUCE（任務整合）                                     │
│  • 收集所有視角的任務清單                                        │
│  • 彙整任務（不去重，各類型獨立）                                │
│  • 應用依賴分析結果                                              │
│  • 生成 Wave 分組                                                │
│  • 識別關鍵路徑                                                  │
│  • 產出統一 tasks.yaml                                           │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│  Phase 6: Memory 存檔                                            │
│  • 建立 tasks/{feature-id}/ 目錄                                 │
│  • 存儲 tasks.yaml + 視角報告                                    │
│  • 更新 index.md                                                 │
│  <!-- METRICS: end_stage stage_id=tasks -->                     │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 觸發命令

```bash
/multi-tasks [plan-id]

# 範例
/multi-tasks user-auth                    # 從 plan ID 載入
/multi-tasks --from-plan user-auth        # 同上
/multi-tasks --quick user-auth            # 快速模式（2 視角）
/multi-tasks --deep user-auth             # 深度模式（6 視角）
```

### 5.3 Flags

```bash
--perspectives N    # 使用 N 個視角（預設 4）
--quick            # 快速模式：2 視角
--deep             # 深度模式：6 視角
--no-memory        # 不存檔到 Memory
--from-plan ID     # 從指定計劃載入
--tdd              # 強制 TDD 順序（測試任務優先）
```

---

## 6. 與 ORCHESTRATE 整合

### 6.1 新階段判斷邏輯

```yaml
stage_detection_v2:
  # 新增：檢查 TASKS 就緒
  check_tasks_status:
    files:
      - "tasks/{id}/tasks.yaml"
    if_exists:
      check_completion:
        if_all_completed: return "REVIEW"
        if_has_pending: return "IMPLEMENT"
    else:
      # 檢查是否有 PLAN
      check_plan: "plans/{id}/implementation-plan.md"
      if_exists: return "TASKS"  # 需要先生成 tasks
```

### 6.2 回退機制

```yaml
rollback_rules:
  # REVIEW BLOCKER → 回到 IMPLEMENT
  review_blocker:
    trigger: "review has BLOCKER"
    target: "IMPLEMENT"
    data:
      affected_tasks: ["T-F-003", "T-F-005"]  # 標記需要修正的任務

  # VERIFY BLOCKED → 回到 IMPLEMENT
  verify_blocked:
    trigger: "verify returns BLOCKED"
    target: "IMPLEMENT"
    data:
      affected_tasks: ["T-F-004"]

  # 新增：任務失敗 → 可選回到 TASKS 重新分解
  task_decomposition_failed:
    trigger: "task fails repeatedly and needs redesign"
    target: "TASKS"
    action: "replan_single_task"
```

### 6.3 Worktree 時機調整

```yaml
worktree_creation:
  # 原本：PLAN 完成後
  # 建議：TASKS 完成後
  trigger: tasks_complete
  reason: "任務定義完成後，才有明確的開發範圍"
```

---

## 7. Memory 輸出結構

```
.claude/memory/tasks/{feature-id}/
├── meta.yaml                 # 元數據
│   ├── feature: 功能名稱
│   ├── date: 2026-01-25
│   ├── perspectives: [dependency-analyst, task-decomposer, ...]
│   ├── total_tasks: 15
│   └── estimated_duration: 8h
├── overview.md               # 一頁概述
├── perspectives/             # 各視角報告
│   ├── dependency-analyst.md
│   ├── task-decomposer.md
│   ├── test-planner.md
│   └── risk-preventor.md
├── tasks.yaml                # 任務定義（主輸出）
├── dependency-graph.md       # 依賴圖（含 Mermaid）
├── execution-plan.md         # 執行計劃
└── status.yaml               # 執行狀態（IMPLEMENT 更新）
```

---

## 8. 輸出範例

```
✅ 任務分解完成：user-auth

📊 分解摘要：
   - 4 個視角完成
   - 15 個任務產出
   - 3 個 Wave 分組
   - 預估總時長：8h

📋 任務分佈：
   - 功能任務 (T-F-*): 6 個
   - 測試任務 (TEST-*): 5 個
   - 風險任務 (RISK-*): 4 個

🎯 優先級分佈：
   - P0 必做: 8 個
   - P1 應做: 5 個
   - P2 可延: 2 個

🔗 關鍵路徑：
   TEST-001 → T-F-001 → T-F-002 → T-F-003
   預估：4h

📁 已存檔：
   .claude/memory/tasks/user-auth/tasks.yaml

🔗 快速連結：
   - 任務清單：tasks.yaml
   - 依賴圖：dependency-graph.md
   - 執行計劃：execution-plan.md
```

---

## 9. 矛盾分析與解決

### 9.1 已解決矛盾

| 矛盾 | 視角 A | 視角 B | 解決方案 |
|------|--------|--------|----------|
| 獨立階段 vs 整合到 PLAN | 架構分析師（獨立） | 方法論專家（整合） | 採用獨立階段，職責更清晰 |
| 依賴分析時機 | 依賴分析專家（先行） | 視角設計師（並行） | 採用先行，為其他視角提供輸入 |

### 9.2 需進一步驗證

| 待驗證項目 | 建議方式 |
|------------|----------|
| 4 視角是否足夠 | 實際執行 3-5 次後評估 |
| Wave 分組效率 | 測量並行 vs 串行執行時間 |
| tasks.yaml 格式可用性 | IMPLEMENT 階段實際消費測試 |

---

## 10. 實施路線圖

### Phase 1: 基礎結構（必須）
1. 建立 `skills/tasks/` 目錄結構
2. 定義 `tasks.yaml` schema
3. 實作 4 視角的 prompt 模板
4. 建立 `.claude/memory/tasks/` 結構

### Phase 2: 核心邏輯（必須）
5. 實作 PLAN → TASKS 解析器
6. 實作 REDUCE 合併邏輯
7. 實作 Wave 分組演算法
8. 更新 ORCHESTRATE 階段判斷

### Phase 3: 整合優化（建議）
9. 調整 Worktree 創建時機
10. 更新 IMPLEMENT 消費 tasks.yaml
11. 實作任務狀態追蹤
12. 更新文檔和 README

---

## 附錄：視角報告連結

- [架構分析師報告](./perspectives/architecture.md)
- [方法論專家報告](./perspectives/methodology.md)
- [依賴分析專家報告](./perspectives/dependency.md)
- [整合設計師報告](./perspectives/integration.md)
- [視角設計師報告](./perspectives/perspectives.md)
- [業界研究員報告](./perspectives/industry.md)
